/**
 * Central export point for all TypeScript types and interfaces
 *
 * Usage:
 *   import { LyricsResponse, SearchQuery } from '@/types';
 */

export type {
  LyricsResponse,
  GraphQLResponse,
  LyricsProps,
  LyricsFormProps,
  SearchQuery,
} from "./lyrics.types";

export type { ApiError, LoadingState, ApiResponse } from "./common.types";
